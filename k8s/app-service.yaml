---
apiVersion: v1
kind: Service
metadata:
  name: rag-api-service
  namespace: rag-hybrid-search
  labels:
    app: rag-api
spec:
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP
  selector:
    app: rag-api
